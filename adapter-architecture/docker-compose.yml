networks:
  kafka-network:
    name: kafka-network
    driver: bridge

services:
  consumer-api:
    image: wmaschette/api-consumer:latest
    container_name: consumer-api
    networks:
      - kafka-network